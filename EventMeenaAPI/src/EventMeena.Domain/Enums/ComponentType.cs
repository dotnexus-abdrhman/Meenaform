namespace EventMeena.Domain.Enums;

/// <summary>
/// نوع المكون (العنصر)
/// </summary>
public enum ComponentType
{
    // ============================================================
    // أسئلة (1-19)
    // ============================================================
    SingleChoice = 1,       // اختيار واحد (radio buttons)
    MultipleChoice = 2,     // اختيار متعدد (checkboxes)
    ShortText = 3,          // نص قصير
    LongText = 4,           // نص طويل
    Rating = 5,             // تقييم (نجوم/أرقام/إيموجي)
    LinearScale = 6,        // مقياس خطي (1-5, 1-10)
    Date = 7,               // تاريخ
    Time = 8,               // وقت
    Number = 9,             // رقم
    Dropdown = 10,          // قائمة منسدلة
    YesNo = 11,             // نعم/لا
    Email = 12,             // بريد إلكتروني
    Phone = 13,             // رقم جوال
    ChoiceGrid = 14,        // شبكة اختيارات

    // ============================================================
    // عناصر عرض (20-29)
    // ============================================================
    Heading = 20,           // عنوان
    Paragraph = 21,         // فقرة/نص توضيحي
    DisplayImage = 22,      // عرض صورة (للعرض فقط)
    DisplayVideo = 23,      // عرض فيديو (للعرض فقط)
    Divider = 24,           // فاصل
    DisplayLink = 25,       // عرض رابط (للعرض فقط)
    DisplayPdf = 26,        // عرض PDF (للعرض فقط)

    // ============================================================
    // عناصر تفاعلية (30-39)
    // ============================================================
    WordCloud = 30,         // سحابة كلمات
    QA = 31,                // أسئلة وأجوبة
    Poll = 32,              // تصويت

    // ============================================================
    // عناصر رفع ملفات - للمشاركين (40-49)
    // ============================================================
    FileUpload = 40,        // رفع ملف عام
    PdfUpload = 41,         // رفع PDF
    ImageUpload = 42,       // رفع صورة
    VideoUpload = 43,       // رفع فيديو

    // ============================================================
    // عناصر خاصة (50-59)
    // ============================================================
    Signature = 50,         // توقيع إلكتروني
    Table = 51,             // جدول (عادي/حسابي)
    Link = 52               // إدخال رابط
}

